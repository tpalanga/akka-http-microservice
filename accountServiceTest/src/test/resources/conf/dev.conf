
test {
  remote {
    service {
      account {
        host = "localhost"
        protocol = "http"
        port = 8080
      }
      newsletter {
        host = "localhost"
        protocol = "http"
        port = 8081
      }
    }
  }
}