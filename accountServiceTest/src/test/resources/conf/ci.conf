
test {
  remote {
    service {
      account {
        host = ${DOCKER_IP}
        protocol = "http"
        port = 8080
      }
      newsletter {
        host = ${DOCKER_IP}
        protocol = "http"
        port = 8081
      }
    }
  }
}